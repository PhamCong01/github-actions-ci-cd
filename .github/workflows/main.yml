name: first ci cd
on: 
  push:
    paths: 
      - '!README.md'
      - '!docs/**'
      - '**.js'
      - '**.html'
    branches: 
      - 'main'
      - 'release/**'
      - '!dev/**'
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "today=$(date +%y-%m-%d)" >> $GITHUB_ENV
      - run: echo hello world, today is $today